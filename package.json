{
  "name": "emma-site-htmx",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tailwindcss build -o static/css/tailwind.css --minify",
    "buildts": "esbuild ts/**/*.ts --bundle --outdir=static/js --minify --format=esm",
    "watch": "tailwindcss build -o static/css/tailwind.css --watch",
    "watchts": "esbuild ts/**/*.ts --bundle --outdir=static/js --minify --format=esm --watch",
    "templ:build": "templ generate",
    "templ:watch": "templ generate --watch",
    "dev": "concurrently \"npm run templ:watch\" \"npm run watch\"",
    "dev:server": "concurrently \"npm run templ:watch\" \"npm run watch\" \"npm run watchts\" \"air\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/three": "^0.181.0",
    "autoprefixer": "^10.4.21",
    "concurrently": "^9.2.1",
    "esbuild": "^0.27.0",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.18",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "chart.js": "^4.5.1",
    "three": "^0.181.1"
  }
}
